# Core dependencies
# IMPORTANT: PyTorch 2.2.2 with CUDA 12.1 support for L40S GPU
# Do NOT change version - 2.2.2 is stable and well-tested for Vintern-1B
torch==2.2.2
torchvision==0.17.2
# IMPORTANT: transformers==4.38.2 is required for Vintern-1B v3_5 compatibility
# Do NOT use version 4.35.2 or older - will cause model loading errors
transformers==4.38.2
pydantic>=2.0.0
peft>=0.10.0
accelerate>=0.27.0

# Vision and attention libraries
timm>=0.9.0  # For InternVIT models
einops>=0.7.0  # For efficient tensor operations

# Image processing
pillow>=10.0.0
torchvision>=0.15.0

# Utilities
numpy<2  # PyTorch 2.2.2 requires NumPy 1.x - do NOT pin to numpy==2.x
pyyaml>=6.0
numpy>=1.24.0
tqdm>=4.65.0
nltk>=3.8.0
kagglehub>=0.1.0
pandas>=1.5.0
pyarrow>=14.0.0            # parquet (data/labeled.parquet)
scikit-learn>=1.3.0,<1.5  # stratified split helpers (>=1.5 needs numpy 2)
scipy>=1.10.0,<1.13      # paired bootstrap / permutation (>=1.13 needs numpy 2)

# Metrics
sacrebleu>=2.4.0
rouge-score>=0.1.2
# METEOR also needs Java + metrics/meteor/meteor-1.5.jar (bundled)

# Router P(r|Q) — PhoBERT (final-plan P4)
sentence-transformers>=2.2.0
underthesea>=6.0.0        # Vietnamese word segmentation for PhoBERT

# Optional - Logging (uncomment to use)
# wandb>=0.16.0

# Optional - Quantization (uncomment if needed)
# bitsandbytes>=0.42.0

# Optional - Flash Attention (only if GPU supports)
# flash-attn>=2.5.0

