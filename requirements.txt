# Core dependencies
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.27.0

# Image processing
pillow>=10.0.0
torchvision>=0.15.0

# Utilities
pyyaml>=6.0
numpy>=1.24.0
tqdm>=4.65.0
nltk>=3.8.0
kagglehub>=0.1.0

# Optional - Logging (uncomment to use)
# wandb>=0.16.0

# Optional - Metrics (uncomment to install metrics dependencies)
# sacrebleu>=2.4.0
# rouge-score>=0.1.2

# Optional - Quantization (uncomment if needed)
# bitsandbytes>=0.42.0

# Optional - Flash Attention (only if GPU supports)
# flash-attn>=2.5.0

