"""modular-vlm-finetune source package.

Subpackages are imported explicitly by callers (``from src.training import ...``)
so that importing ``src`` never pulls heavy optional dependencies like torch.
"""

__version__ = "0.1.0"
